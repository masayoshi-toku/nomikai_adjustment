%h1 Events
%table
  %thead
    %tr
      %th User
      %th Title
      %th{:colspan => "3"}
  %tbody
    - if @events
      - @events.each do |event|
        %tr
          %td= event.user_id
          %td= event.title
          %td= link_to 'Show', event_path(event.url_path)
%br/
= link_to 'New Event', new_event_path
